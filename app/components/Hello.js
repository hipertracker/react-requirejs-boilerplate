import React from 'react'

const Hello = React.createClass({
    render() {
        return (
            <section>
                Hello World!
            </section>
        )
    }
});

export default Hello;
